version: '3.8'

services:
  boxxed:
    build: .
    container_name: boxxed
    ports:
      - "3001:3001"
    volumes:
      - ./data:/app/data
    environment:
      - PORT=3001
    restart: unless-stopped